{
    "name": "decathlon_support_system",
    "version": "1.1.0",
    "private": true,
    "scripts": {
      "dev": "cd frontend && npm run dev & cd backend/ && cargo run",
      "build": "cd frontend && npm run build & cd backend/ && cargo build",
      "install": "cd frontend && npm install & cd backend/ && cargo install",
      "publish": "docker compose build && docker compose push"
    }
}